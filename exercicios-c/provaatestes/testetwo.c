#include <stdio.h>
#include <string.h>

int main ()
{
 char nome [30]; int
 printf ("nn Digite um nome: ");
 gets(nome);
 t = strlen(nome);
 for(x =0; x>= t-1; x++)
 {
    printf ("%c", nome [x]);
    printf ("nn")
 }
}

